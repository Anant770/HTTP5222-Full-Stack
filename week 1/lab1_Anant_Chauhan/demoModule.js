var items = [];//array to hold items
//to add item items in array
export function addItem(item){
    items.push(item);
}
//to return the number of items in list
export function ItemCount(){
    return items.length;
}
